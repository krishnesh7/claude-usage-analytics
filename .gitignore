# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual env
.venv/
venv/
env/

# Generated reports (may contain prompt content — never commit)
claude-usage-report-*.html
report-*.html
*_report_preview.html

# Personal usage tracking (local cost accounting — not for sharing)
docs/usage/CLAUDE_USAGE.md

# Personal Medium article drafts/research — not for the repo
PROJECT_ANALYSIS.md
EXISTING_CONTENT_RESEARCH.md
DIFFERENTIATION_MATRIX.md
ARTICLE_POSITIONING.md
MEDIUM_ARTICLE_OUTLINE.md
MEDIUM_ARTICLE_DRAFT.md

# Local databases / caches (live outside repo, but guard anyway)
*.db
*.db-wal
*.db-shm

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Self-contained plugin data dir (DB, config copies, bootstrap log)
data/

# Parser Node deps — rebuilt per-machine on bootstrap (native better-sqlite3 binary)
parser/node_modules/
node_modules/
