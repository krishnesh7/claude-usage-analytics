---
description: Show the claude-usage-tracker getting-started guide — setup steps, slash commands, CLI reference, and tips for reading CLAUDE_USAGE.md.
allowed-tools: Bash
---

Display the getting-started guide for the claude-usage-tracker plugin.

Run:
```bash
cat "${CLAUDE_PLUGIN_ROOT}/docs/GETTING_STARTED.md"
```

Render the output as formatted Markdown for the user. After displaying it, ask if they want to jump straight to any specific step (project registration, reading their CLAUDE_USAGE.md, or a CLI command walkthrough).
