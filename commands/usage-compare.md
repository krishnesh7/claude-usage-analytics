---
description: Compare claude-usage-tracker with the official session-report plugin — feature table, architecture differences, and when to use each.
allowed-tools: Bash
---

Display the feature comparison document for the claude-usage-tracker plugin.

Run:
```bash
cat "${CLAUDE_PLUGIN_ROOT}/docs/COMPARISON.md"
```

Render the output as formatted Markdown. After displaying it, ask the user if
there is a specific capability they want to explore or enable (e.g. cost
imputation, project registration, SDLC stage tagging, or the live dashboard).
